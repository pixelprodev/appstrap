class Fixtures {
  constructor (config) {
    this.collection = new Map()
    this.update = this.mapCollection
  }

  mapCollection (config) {

  }
}

class Fixture {
  constructor ({ mode, endpoints }) {

  }
}

module.exports = Fixtures
